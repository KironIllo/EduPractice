@startuml
skinparam shadowing false
skinparam componentStyle uml2

' Объекты (Узлы)
node "Пользователь" as Client
node "Веб-приложение" as DjangoApp
node "Система аутентификации" as DjangoAuth
node "БД" as Database

' --- Сценарий: Вход пользователя (POST запрос) ---

' 1. Пользователь отправляет данные для входа в приложение
Client -- DjangoApp : 1: Вход(данные формы) >\n8: HTTP Перенаправление (next_url/home) <

' 2. Приложение передает данные в систему аутентификации для проверки
DjangoApp -- DjangoAuth : 2: authenticate(username, password) >\n5: Возвращен объект User / None <

' 3. Система аутентификации запрашивает учетные данные из базы данных
DjangoAuth -- Database : 3: Запрос на проверку учетных данных >\n4: Результат проверки <

' 6. Если пользователь найден, приложение вызывает функцию login для установки сессии
DjangoApp -- DjangoAuth : 6: login(request, user) >\n7: Сессия пользователя установлена <

' --- Сценарий: Выход пользователя (GET запрос) ---

' 9. Пользователь инициирует выход из системы
Client -- DjangoApp : 9:Выход из системы >\n12: HTTP Перенаправление (/) <

' 10. Приложение вызывает функцию logout для завершения сессии
DjangoApp -- DjangoAuth : 10: logout(request) >\n11: Сессия пользователя очищена <

@enduml
