@startuml
|Пользователь|
start
:Заполнить форму (username, password);
:Нажать "Войти";

|Система (login_view)|
:Валидация формы через AuthenticationForm;
if (Форма валидна?) then (да)
  :authenticate(username, password);
  if (Пользователь существует и пароль верен?) then (да)
    :login(request, user);
    :Перенаправить на главную страницу;
  else (нет)
    :Показать ошибку "Неверные учётные данные";
    :Остаться на странице логина;
  endif
else (нет)
  :Показать ошибки формы;
  :Остаться на странице логина;
endif
stop
@enduml