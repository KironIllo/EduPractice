@startuml
|Функция get_currency_price|
start
:Получить currency_id;
:Сформировать текущую дату (день, месяц, год);
:Построить URL для запроса;
:Выполнить GET-запрос к API ЦБ;

|Внешний API (cbr.ru)|
:Обработать запрос;
:Вернуть XML с курсами валют;

|Функция get_currency_price|
if (Статус ответа OK?) then (да)
  :Прочитать XML в DataFrame;
  :Преобразовать DataFrame в словарь;
  :Перебрать все валюты в поисках currency_id;
  if (Валюта найдена?) then (да)
    :Извлечь name, rate, nominal, char_code;
    :Вернуть словарь с данными;
  else (нет)
    :Записать Ошибку в лог;
    :Вернуть None;
  endif
else (нет)
  :Записать ошибку запроса в лог;
  :Вернуть None;
endif
stop
@enduml